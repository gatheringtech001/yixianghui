<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.ContentStatusMapper">
    <update id="updateGoodsStatus">
        update app_goods set status = #{status}, update_time = now() where goods_id = #{id}
    </update>

    <update id="updateActivityStatus">
        update app_activity set status = #{status}, update_time = now() where activity_id = #{id}
    </update>

    <update id="updateArticleStatus">
        update app_article set status = #{status}, update_time = now() where article_id = #{id}
    </update>

    <update id="updateAdPositionStatus">
        update app_ad_position set status = #{status} where position_id = #{id}
    </update>

    <update id="updateAdContentStatus">
        update app_ad_content set status = #{status} where content_id = #{id}
    </update>
</mapper>
